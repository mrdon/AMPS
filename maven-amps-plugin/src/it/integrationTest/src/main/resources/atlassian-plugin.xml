<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">

    <plugin-info>
        <description>Test IT Plugin</description>
        <version>1</version>
    </plugin-info>

    <servlet name="ItServlet" class="com.atlassian.sdk.test.it.ItServlet" key="it">
        <url-pattern>/it</url-pattern>
    </servlet>

</atlassian-plugin>
