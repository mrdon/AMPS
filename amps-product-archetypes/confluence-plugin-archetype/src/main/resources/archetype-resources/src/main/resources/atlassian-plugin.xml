#set( $dollar = '$' )
<atlassian-plugin key="\${groupId}.\${artifactId}" name="\${artifactId}" plugins-version="2">
    <plugin-info>
        <description>${dollar}{project.description}</description>
        <version>${dollar}{project.version}</version>
        <!-- TODO: Add vendor details -->
        <!--
        <vendor name="Example Company" url="http://www.example.com"/>
        -->
    </plugin-info>

    <macro name="${artifactId}" class="${groupId}.ExampleMacro" key="my-macro">
    <!-- TODO: Add macro description -->
    <!-- <description></description> -->
    </macro>
</atlassian-plugin>
