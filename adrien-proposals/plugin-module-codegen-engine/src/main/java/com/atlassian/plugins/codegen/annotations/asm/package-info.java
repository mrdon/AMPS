/**
 * Provides the main ASM parser to read product-specific module creator annotations and register them so they can be
 * looked up by the engine.
 *
 * This is where "the magic" happens to link module creaotrs with specific products.
 *
 * @since 3.6
 *
 */
package com.atlassian.plugins.codegen.annotations.asm;